<nav class="sidebar bg-white border-r border-gray-200 fixed top-0 left-0 h-full z-30 transition-transform duration-200 ease-in-out
  transform -translate-x-full md:translate-x-0 md:static md:block"
  id="sidebar">
  <ul class="flex flex-col gap-4 mt-8">
    <li>
      <a href="/<%= user && user.role ? user.role : '' %>"
         class="block px-4 py-2 rounded-md font-medium text-gray-800 hover:bg-blue-50 hover:text-blue-600 transition
         <%= (typeof activeTab !== 'undefined' && activeTab === 'home') ? ' bg-blue-100 text-blue-700' : '' %>">
        Home
      </a>
    </li>
    <!-- Future: Add more dynamic links here based on user.role and available tabs -->
    <li>
      <a href="/logout"
         class="block px-4 py-2 rounded-md font-medium text-gray-800 hover:bg-red-50 hover:text-red-600 transition">
        Logout
      </a>
    </li>
  </ul>
</nav>
