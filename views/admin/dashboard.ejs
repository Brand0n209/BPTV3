<%- include('../partials/header', { title: 'Admin Dashboard - Bright Prodigy' }) %>
<div class="flex min-h-screen w-full group">
  <%- include('../partials/sidebar', { user: typeof user !== 'undefined' ? user : null, activeTab: 'home' }) %>
  <div class="flex-1 flex flex-col">
    <%- include('../partials/topbar', { user: typeof user !== 'undefined' ? user : null, currentTab: 'Home' }) %>
    <div class="main-container">
      <%- include('home', { user: typeof user !== 'undefined' ? user : null }) %>
    </div>
    <%- include('../partials/footer') %>
  </div>
</div>
