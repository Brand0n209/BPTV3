<%- include('../partials/header', { title: 'Admin – Settings' }) %>
<div class="layout-grid min-h-screen h-screen">
  <%- include('../partials/sidebar', { user, activeTab: 'settings' }) %>
  <div class="main-content flex-1 flex flex-col min-h-screen h-screen" style="min-width:0;">
    <%- include('../partials/topbar', { user, currentTab: 'Settings' }) %>
    <div class="tab-content w-full max-w-full px-4 flex-1 flex flex-col overflow-hidden">
      <h2 class="text-xl font-semibold mb-4">Settings</h2>
      <div class="content-area flex-1 flex flex-col overflow-hidden">
        <div class="w-full border rounded bg-gray-50 p-4">
          <h3 class="text-lg font-semibold mb-3">[Settings Placeholder]</h3>
          <div class="text-gray-500 text-center py-8">[Settings form or options will appear here]</div>
        </div>
      </div>
    </div>
    <%- include('../partials/footer') %>
  </div>
</div>
