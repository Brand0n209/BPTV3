<%- include('../partials/header', { title: 'Admin – Cust Payout' }) %>
<div class="layout-grid min-h-screen h-screen">
  <%- include('../partials/sidebar', { user, activeTab: 'custPayout' }) %>
  <div class="main-content flex-1 flex flex-col min-h-screen h-screen" style="min-width:0;">
    <%- include('../partials/topbar', { user, currentTab: 'Cust Payout' }) %>
    <div class="tab-content w-full max-w-full px-4 flex-1 flex flex-col overflow-hidden">
      <h2 class="text-xl font-semibold mb-4">Customer Payout</h2>
      <div class="content-area flex-1 flex flex-col overflow-hidden">
        <div class="table-container overflow-x-auto w-full border rounded bg-gray-50 p-4">
          <h3 class="text-lg font-semibold mb-3">[Payout Table Placeholder]</h3>
          <table class="min-w-full">
            <thead>
              <tr>
                <th class="px-4 py-2 text-left">Customer</th>
                <th class="px-4 py-2 text-left">Amount</th>
                <th class="px-4 py-2 text-left">Status</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td class="px-4 py-2 text-gray-500" colspan="3">[Table of payouts will appear here]</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
    <%- include('../partials/footer') %>
  </div>
</div>

