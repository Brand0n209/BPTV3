<%- include('../partials/header', { title: 'Admin – Subs' }) %>
<div class="layout-grid">
  <%- include('../partials/sidebar', { user, activeTab: 'subs' }) %>
  <div style="width:100%">
    <%- include('../partials/topbar', { user }) %>
    <div class="main-container">
      <div class="w-full max-w-3xl mx-auto">
        <div class="flex flex-wrap gap-2 mb-6">
          <% subTabs.forEach(function(tab) { %>
            <a href="/admin/subs/<%= encodeURIComponent(tab.value) %>"
              class="px-4 py-2 rounded-full text-sm font-medium border transition
                <%= filterType === tab.value ? 'bg-blue-600 text-white border-blue-600' : 'bg-white text-blue-700 border-blue-300 hover:bg-blue-50' %>">
              <%= tab.label %>
            </a>
          <% }) %>
        </div>
        <% if (submissions && submissions.length > 0) { %>
          <div class="overflow-x-auto">
            <table class="min-w-full bg-white border border-gray-200 rounded-lg shadow">
              <thead>
                <tr>
                  <% Object.keys(submissions[0]).forEach(function(col) { %>
                    <th class="px-4 py-2 border-b text-left text-xs font-semibold text-gray-700 bg-gray-50"><%= col %></th>
                  <% }) %>
                </tr>
              </thead>
              <tbody>
                <% submissions.forEach(function(row) { %>
                  <tr>
                    <% Object.values(row).forEach(function(val) { %>
                      <td class="px-4 py-2 border-b text-sm text-gray-800"><%= val %></td>
                    <% }) %>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } else { %>
          <div class="text-center text-gray-500 py-12">
            <svg class="mx-auto mb-2 w-10 h-10 text-blue-200" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 17v-2a4 4 0 018 0v2m-4-4a4 4 0 00-4-4V7a4 4 0 018 0v2a4 4 0 00-4 4z" /></svg>
            <p>No leads in this category yet.</p>
          </div>
        <% } %>
      </div>
    </div>
    <%- include('../partials/footer') %>
  </div>
</div>
