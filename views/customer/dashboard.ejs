<%- include('../partials/header', { title: 'Customer Dashboard - Bright Prodigy' }) %>
<div class="layout-grid">
  <%- include('../partials/sidebar') %>
  <div style="width:100%">
    <%- include('../partials/topbar', { user }) %>
    <div class="main-container">
      <div class="card">
        <h2>Customer Dashboard</h2>
        <p>Welcome, <%= user && user.firstName ? user.firstName : 'Customer' %>! You are logged in as <strong>Customer</strong>.</p>
        <!-- Future: Add customer dashboard tabs and widgets here -->
      </div>
    </div>
    <%- include('../partials/footer') %>
  </div>
</div>
